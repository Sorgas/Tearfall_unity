using Leopotam.Ecs;
using UnityEngine;

namespace game.view.ui.tooltip {
// used when player right-clicks map tile while unit is selected. Does not belong to trigger/tooltip hierarchy.
public class RightClickTooltipHandler : MonoBehaviour {
    public void fill(EcsEntity unit, Vector3Int position) {
        
    }
}
}